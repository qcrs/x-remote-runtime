# History

The formal development path now starts at this repository's validated Gate 8D
baseline. Earlier material is retained as read-only history:

- `/home/lvtong/corex-feasibility` contains the original compiler, protocol,
  registration, and Runtime experiments.
- `/home/lvtong/remote/corex-feasibility` contains earlier client-side
  baselines.
- `docs/gate8/` contains the Gate 8 design freezes, validation records, and
  historical patch scripts carried with this repository.
- `evidence/gate8d/` contains the preserved Gate 8D generated artifacts and
  validation archive.

Future Runtime changes belong in `/home/lvtong/corex-remote-runtime` and
should be described by current architecture and state documents rather than
by creating another Gate snapshot directory.
